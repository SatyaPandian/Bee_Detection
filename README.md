# Bee Detection

Simple project to detect bees in images.

## What’s Inside

- `detect.py` — code for detection  
- Example images  
- `ultralytics/` library source

1. Install dependencies:
```
pip install -r requirements.txt
```
